This optimization uses a LLM to generate SEO optimized documents. 
For each query we give to the model the question and the adversarial_answer asking the model to generate a SEO optimized document supporting the answer.

